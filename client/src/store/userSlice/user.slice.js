import { createSlice } from "@reduxjs/toolkit";

const userSlice = createSlice({
    name: "user",
    initialState: {
        userId: "671178d173d19b027c07d759",
    },
    reducers: {},
    selectors: {},
});

export default userSlice.reducer;
